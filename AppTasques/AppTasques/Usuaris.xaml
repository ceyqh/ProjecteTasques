<Window x:Class="AppTasques.Usuaris"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:AppTasques"
        mc:Ignorable="d"
        Title="Usuaris" Height="550" Width="300">
    <Window.Resources>
        <Style x:Key="HoverButton" TargetType="Button">
            <Setter Property="Background" Value="#FF272727"/>
            <Setter Property="Foreground" Value="#FFEFEFEA"/>
            <Setter Property="BorderBrush" Value="#FF272727"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">

                        <Border x:Name="border"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    SnapsToDevicePixels="True">

                            <ContentPresenter
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center"
                        Margin="0"
                        RecognizesAccessKey="True"/>
                        </Border>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#FFEFEFEA"/>
                                <Setter Property="Foreground" Value="#FF272727"/>
                                <Setter Property="BorderThickness" Value="1"/>
                            </Trigger>
                        </ControlTemplate.Triggers>

                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    
    <Grid Background="#FFEFEFEA">
        <StackPanel Margin="10"  Background="#FFEFEFEA">
            <TextBlock FontWeight="Bold" Margin="0, 0, 0, 10" FontSize="30">Usuaris:</TextBlock>
            <ListBox x:Name="llistaUsuaris" Height="200" Background="#FFEFEFEA" FontSize="15"></ListBox>

            <TextBlock Margin="0, 10, 0, 10" FontSize="15">Nom:</TextBlock>
            <TextBox x:Name="nouNom" Margin="0,0,0,10" FontSize="15" Background="#FFEFEFEA" BorderBrush="#272727" Foreground="#FF272727"/>

            <TextBlock Margin="0, 0, 0, 10" FontSize="15">Contrasenya:</TextBlock>
            <PasswordBox x:Name="nouContrasenya" Margin="0,0,0,10" FontSize="15" Background="#FFEFEFEA" BorderBrush="#272727" Foreground="#FF272727"/>

            <TextBlock Margin="0, 0, 0, 10" FontSize="15">Confirmar contrasenya:</TextBlock>
            <PasswordBox x:Name="nouConfirmarContrasenya" Margin="0,0,0,10" FontSize="15" Background="#FFEFEFEA" BorderBrush="#272727" Foreground="#FF272727"/>

            <Button FontSize="15" Width="80" Style="{StaticResource HoverButton}" Click="AfegirUsuari" Margin="0, 10, 0, 0" >Afegir</Button>

        </StackPanel>        
    </Grid>
</Window>
